<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tedu.mapper.CartMapper">

    <select id="queryOne" parameterType="com.tedu.pojo.Cart" resultType="com.tedu.pojo.Cart">
        select * from t_cart where user_id=#{userId}
        and product_id=#{productId};
    </select>
    <insert id="save" parameterType="com.tedu.pojo.Cart">
        insert into t_cart (
        user_id,product_id,product_name,
        product_image,product_price,num) values(
        #{userId},#{productId},#{productName},
        #{productImage},#{productPrice},#{num})
    </insert>
    <update id="update" parameterType="com.tedu.pojo.Cart">
        update t_cart
        set num=#{num} where user_id=#{userId}
        and product_id=#{productId};
    </update>
</mapper> 












