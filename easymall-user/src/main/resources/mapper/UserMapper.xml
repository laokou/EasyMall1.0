<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tedu.mapper.UserMapper">
    <insert id="regist" parameterType="com.tedu.pojo.User">
        insert into t_user (
        user_id,
        user_name,
        user_password,
        user_email,
        user_nickname,
        user_type) values(
        #{userId},
        #{userName},
        #{userPassword},
        #{userEmail},
        #{userNickname},
        #{userType})
    </insert>
</mapper> 












